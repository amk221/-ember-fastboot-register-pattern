{{yield (hash
  child=(component "my-child"
    siblings=children
    active-index=activeChildIndex
    on-activate=(action "activateChildAtIndex")
    on-register=(action "registerChild")
    on-deregister=(action "deregisterChild"))
)}}
{{yield
  (hash
    child=(component "my-child"
    siblings=this.children
    activeIndex=this.activeChildIndex
    onActivate=(action "activateChildAtIndex")
    onRegister=(action "registerChild")
    onDeregister=(action "deregisterChild"))
  )
}}